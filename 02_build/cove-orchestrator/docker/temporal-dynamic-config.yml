# Temporal dynamic configuration
# Tune these as we learn actual workloads
frontend.keepAliveMaxConnectionAge:
  - value: 5m
system.forceSearchAttributesCacheRefreshOnRead:
  - value: true
limit.maxIDLength:
  - value: 255
